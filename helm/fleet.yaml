namespace: pihole

helm:
  releaseName: pihole
  valuesFiles:
    - values.yaml
    - values/k3s-rpi-values.yaml

- name: prod
  maxUnavailable: 50%
  clusterSelector:
    matchLabels:
      env: prod
      clusterName: k3s-rpi
